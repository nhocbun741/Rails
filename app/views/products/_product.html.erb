<tr>
<td><%= product.title%></td>
    <td><%= product.description%></td>
    <td><%= product.price%></td>
    <td><%= product.category.try(:title)%></td>
      <td><%= link_to "Details", product %></td>
    <td><%= link_to "Edit", edit_product_path(product) %> <%= link_to "Delete", product_path(product), method: :delete, data:{confirm: 'Are you sure delete'}%></td>
</tr>